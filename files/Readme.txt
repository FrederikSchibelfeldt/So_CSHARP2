To change the input of the files see line 19. To change the output file see lines 29,32 and 37. After this the program can be run as any c# console
application by either doing it from an IDE or from the command line with dotnet run command. 

The solution examples are LargeSolutionExample and smallSolutionExample. The inputs used for these are the given inputs large.xml and small.xml. 

The same parameters where used for both solution examples.

